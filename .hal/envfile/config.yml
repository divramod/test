template_mappings:
  - environment: remote-production
    templates:
      - envfiles/.env.template.base
      - envfiles/.env.template.remote-production
    targets:
      - .env.remote-production
  - environment: local-development
    templates:
      - envfiles/.env.template.base
      - envfiles/.env.template.local-development
    targets:
      - .env.local-development
  - environment: worktree
    templates:
      - envfiles/.env.template.base
      - envfiles/.env.template.worktree
    targets:
      - .env.local-development
